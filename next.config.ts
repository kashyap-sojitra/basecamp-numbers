import type { NextConfig } from "next";

/**
 * Intentionally empty. Next 16's defaults are the configuration: App Router,
 * React's own view transitions, Tailwind through PostCSS, and nothing that
 * needs a rewrite, a header rule or an image loader. The file stays because
 * Next looks for it; a setting belongs here only once something needs it.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
